# format:
# <nixpkgs-attr> <commands-to-verify...>
# nixpkgs-attr は `nix-env -iA <attr>` に渡される

# System tools
nixpkgs.git      git
nixpkgs.curl     curl
nixpkgs.wget     wget
nixpkgs.gnupg    gpg
nixpkgs.podman   podman
nixpkgs.neovim   nvim
nixpkgs.gh       gh

# Runtimes
nixpkgs.nodejs_24  node npm npx
nixpkgs.go         go

# AI CLI tools
nixpkgs.claude-code   claude
nixpkgs.openai-codex  codex
nixpkgs.gemini-cli    gemini
